# Variables para docker compose (copie a .env y cambie los valores)
DB_PASSWORD=inventario
# Genere secretos con: openssl rand -hex 48
JWT_ACCESS_SECRET=
JWT_REFRESH_SECRET=
CORS_ORIGINS=http://localhost:8080
